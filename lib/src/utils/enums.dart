// ignore_for_file: constant_identifier_names

enum CountrySelectionState { Selected, NotSelected }

enum Region {
  Africa,
  Antarctica,
  Asia,
  Australia,
  Europe,
  NorthAmerica,
  SouthAmerica,
  Oceania,
  Unknown
}

enum CountrySelectorStyle { menu, sheet }

enum CountrySelectorType { country, state }

enum CountrySelectorValidateMode { autoValidate, onChanged }
